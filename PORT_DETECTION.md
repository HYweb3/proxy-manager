# 端口自动检测和切换功能

## 🎯 功能说明

Proxy Manager Web 服务现在具备智能端口检测功能，可以自动检测端口占用并切换到可用端口。

## 🔍 工作原理

### 1. 启动前检测
- 默认尝试使用端口 **5080**
- 如果 5080 被占用，自动尝试 5081, 5082, 5083... (最多尝试 10 个端口)
- 找到可用端口后使用该端口启动服务

### 2. 多种检测方法
支持多种端口检测方法，确保兼容性：
- `netstat` - 传统网络工具
- `ss` - 现代网络工具
- TCP 连接测试 - 备用方法

### 3. 智能提示
- ✅ 端口可用时显示绿色确认
- ⚠️ 端口切换时显示警告和新端口号
- ❌ 无法找到可用端口时显示错误信息

## 📋 使用场景

### 场景1: 正常启动
```bash
🔍 检查端口可用性...
✅ 端口 5080 可用
🚀 Proxy Manager Web界面启动中...
📋 管理面板: http://0.0.0.0:5080
```

### 场景2: 端口被占用
```bash
🔍 检查端口可用性...
⚠️ 端口 5080 被占用，使用端口 5081
🚀 Proxy Manager Web界面启动中...
📋 管理面板: http://0.0.0.0:5081
```

### 场景3: 多个端口被占用
```bash
🔍 检查端口可用性...
⚠️ 端口 5080 被占用，使用端口 5083
🚀 Proxy Manager Web界面启动中...
📋 管理面板: http://0.0.0.0:5083
```

## 🛠️ 测试功能

### 快速测试
```bash
# 测试端口检测功能
bash test-port-detection.sh
```

### 手动检查端口占用
```bash
# 检查 5080-5085 端口占用情况
for port in {5080..5085}; do
    lsof -i :$port || echo "Port $port is free"
done

# 查看占用端口的进程
lsof -i :5080
```

### 清除端口占用
```bash
# 查找并杀死占用 5080 端口的进程
PID=$(lsof -ti :5080)
kill -9 $PID

# 或者使用 systemctl (如果服务已安装)
systemctl stop proxy-web
```

## 🔧 配置文件

服务启动后会保存实际使用的端口到配置文件：
```bash
/etc/proxy-manager/actual_port.txt
```

## 🌐 访问地址

启动成功后，根据提示使用实际端口号访问：
- 本地访问: `http://localhost:实际端口`
- 网络访问: `http://服务器IP:实际端口`

## 💡 故障排除

### 问题: 无法找到可用端口
**解决方案:**
1. 检查是否有大量程序占用连续端口
2. 手动清理端口占用: `lsof -ti :5080 | xargs kill -9`
3. 重启系统释放端口资源

### 问题: 端口检测不准确
**解决方案:**
1. 确保系统有 `netstat` 或 `ss` 工具
2. 检查防火墙是否影响端口检测
3. 使用 `lsof` 手动验证端口状态

### 问题: 服务启动后无法访问
**解决方案:**
1. 检查实际使用的端口号（查看启动信息）
2. 检查防火墙是否开放了实际端口
3. 查看服务日志: `journalctl -u proxy-web -f`

## 🎉 优势

1. **自动解决冲突**: 无需手动处理端口占用
2. **提高成功率**: 多端口尝试增加启动成功率
3. **清晰提示**: 明确显示实际使用的端口
4. **智能降级**: 从默认端口自动寻找替代方案
5. **环境适应**: 适应不同的服务器环境配置

## 📊 性能影响

- 端口检测耗时: < 1秒
- 内存占用: 几乎无增加
- 启动时间: 基本无影响

这个功能大大提高了 Proxy Manager 的健壮性和用户体验！